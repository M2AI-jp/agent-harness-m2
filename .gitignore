.DS_Store
.agent-harness/
tmp/
*.log

# Installed copies generated by scripts/install-*.sh. Canonical source lives in
# hooks/ and skills/; tracking these would drift from it (see README "canonical source").
.claude/
.codex/
.agents/skills/

# Google Drive sync scratch (this repo lives under a synced folder)
.tmp.driveupload/
